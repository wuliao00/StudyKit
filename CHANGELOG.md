# 更新日志

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [2.2.0] - 2026-09-21

### 新增
- **设置页**（`settings`，入口是学习页右上角齿轮）：一段三节同页滚动，改一格立刻落库，没有"保存"按钮
  - 外观与动效：主题三态（跟随系统 / 浅色 / 深色）、玻璃档位、减弱动效
  - 档案与目标：昵称、每日目标词数、复习提醒周期、考试日（`yyyy-MM-dd` 校验，非法就地报错）
  - 数据管理：一键导出备份、从备份恢复、占用统计、清除学习数据、诊断（上次词库失败原因）
- **本地信息持久化**：数据库升到 Room v4（新增 `app_settings` 键值表），`MIGRATION_3_4` 是纯加表，
  既有十张表一个字段都不动。设置项读回时对坏值**逐项**退回默认，而不是整份失败
- **手绘玻璃材质**（`ui/material/Glass.kt`）：半透明底 + 顶部入射光 + 一条随事件扫过的亮带 + 上亮下暗内描边。
  挂在三处真浮层上：底栏、打卡弹层、词库失败状态条
- **一键导出 / 恢复**：zip = `manifest.json` + `studykit.db` + 错题图片目录。导出前先 `wal_checkpoint`，
  恢复前先在临时文件上校验 CRC（不过就一个字节都不动），成功后重启进程
  —— 数据库连接池还握着旧 inode，不重启会出现"恢复完成但界面还是旧数据、下一次写入把刚恢复的库盖回去"

### 变更
- **词库下载改走 HTTPS 镜像**（`util/importer/DictMirror.kt`）：目录给的是明文 `http://<bucket>.nos.netease.com/…`，
  现在按「先镜像、后原址」两跳试。`network_security_config.xml` 的明文例外降级为只服务回落那一跳
- 网络层**自己跟重定向**（`instanceFollowRedirects = false`）：跟之前 3xx 对上层不可见，
  未认证门户把明文请求 302 进登录页时，App 会读到一页 HTML 去解 zip，报成"压缩包里没有找到词表 JSON"，
  和真·坏包同一个症状。现在落到非预期主机会直接说"被网络重定向到了 x.x.x.x，通常是网页认证或代理接管"
- 卡片的描边从均匀一圈换成**上实下淡**的竖向渐变（暖纸 + 微光），卡面保持不透明：
  M1 那批 WCAG AA 数字全按实底算，卡片一透明就全体失效
- `RingGauge` 的两枚 `Stroke` 提到组合层缓存（冷启动时所有环同时补间，原来每帧各分配一次）
- 「减弱动效」在 `ConfettiBurst` / `StaggeredIn` / `rememberPressScale` 三处**早退**：
  是不跑，不是"跑了看不见"，一次重绘都不产生
- 复习提醒周期不再写死 6 小时。首碰 WorkManager 用 `KEEP`、只有用户真改了周期才 `REPLACE`——
  每次冷启动都 REPLACE 会把倒计时清零，天天开 App 的人永远等不到提醒
- 数据层补齐 11 个全表删除口（习惯 / 打卡 / 书目 / 摘录 / 书评 / 题目 / 刷题 / 词 / 复习明细 / 词库 / 错题），
  「清除学习数据」因此是十张表一起清，不会留下"复习记录挂在已删除的词上"这种悬空行

### 已知限制
- 玻璃是**手绘**的，不是真折射：验收机（vivo V2156A）是 Android 11，系统级背景模糊 `RenderEffect` 要 API 31，
  Compose 的 `Modifier.blur` 在 11 以下是空操作，所以那套实现留在 Android 12+ 也用不上，本版不做叠加分支
- 「高帧率」这一版本能兑现的是**稳定满 60 帧**：验收机 `supportedModes` 只有 `fps=60.0` 一档，
  90/120Hz 的 `setRequestedFrameRate` 是 API 31+，本机无法验证，未引入
- 恢复备份要求 zip 里的库版本不高于当前 App；高版本库恢复到本版会打不开，未做闸门
- `backup_rules.xml` 仍把数据库排除在系统备份之外，本版新增的是**应用内**导出，不是云备份

## [2.1.0] - 2026-09-21

### 新增
- **批量粘贴录入**：一行一条，Tab / 逗号 / 两个空格 / 单空格四种分列自动识别，第三列可选作例句；
  边打边解析（250ms 防抖，解析跑在后台线程，300 行也不掉帧）。题目与摘录同样各有一个行解析器
  （题目支持「字母答案」与「选项原文答案」两种写法、可选解析列；摘录按首尾竖线定界，摘录里的竖线不会被切碎）
- **文件导入**：录入页「选文件」走 SAF 选文本文件，UTF-8 BOM 与 CRLF 自动剥掉，与粘贴共用同一套解析器
- **预览-入库三屏**：粘贴 / 文件 / 词库 / OCR 四条来源汇到同一套「预览 → 导入 → 结果」。
  预览页逐行可勾选剔除（删除线回显）、坏行进「待修正」区并说明原因（列数不足 / 内容为空 / 答案无法识别），
  **任何一行都不会被静默丢弃**；结果页给出成功 / 重复跳过 / 待修正三个数，重复与待修正都能展开看逐条明细
- **在线词库商店**：`study/dict/store` 列出 81 本公开词表（书名/标签搜索、每本独立下载进度条），
  下载后仍走预览-入库流程，并登记成一本可**整本撤销**的词库（撤销只删这本带进来的词，手工录入不受影响）。
  ⚠️ 下载与撤销这条链路**未在真机验通**，见下方「已知限制」
- **截图 OCR 取词**：ML Kit bundled 中文模型，**全离线、不需要 Google 服务**（vivo 可直接用）。
  错题「拍照录入」页一键「从图片提取文字」，首行进标题、其余进备注且仍可编辑；
  单词库「截图取词」选一张一行一词的截图即可批量成词

### 变更
- **APK 体积**：bundled OCR 的原生库带四个 ABI 共 41MB，直接把 debug 包推到 56.9MB。
  本仓是直接发 APK（不走商店的 per-device split），因此只保留 `arm64-v8a` / `armeabi-v7a`，
  回到 **33.7MB**。代价：x86_64 模拟器装不上 OCR 功能
- 数据库升到 Room v3（新增 `word_lists` 一本词库一行，`words.source_list_id` 指回它）；
  迁移 `MIGRATION_2_3` 的执行与 schema 校验已在真机验证
- 错题录入页支持从系统分享直接进入（清单已注册 `SEND` / `SEND_MULTIPLE`，仅该图片域名放行明文 HTTP）

### 数据来源与许可
- 在线词库的词表来自开源仓库 [kajweb/dict](https://github.com/kajweb/dict)（其数据抓取自公开背词应用），
  **仅用于个人学习**；App 内置的是目录快照，导入动作在你手机上完成，**导入后完全离线，不上传任何数据**

### 已知限制
- **同签名 A→B 覆盖安装**这一条分发路径未实测（本机与 CI 都没有稳定 keystore）；
  同因，**升级后原有数据行完好保留**也未实测（验证时注入的是 schema-only 重放库，行数恒为 0）
- OCR 对低质量截图、手写体、竖排文字识别率有限，识别结果一律需人工过一眼再入库
- 词库目录是随版本打包的快照，新书与勘误需随版本更新
- **多选题暂不支持**（模型只有一个正确答案位），这类行会进「待修正」并提示
- **摘录的批量导入未实现**：解析器已就绪，但 `excerpts` 要求指定所属的书、且没有承载「感想」的列，
  需要先定产品口径（导进哪本书、感想存哪儿），故本期只开放单词与题目两种批量导入
- **预测式返回**已打开开关（`enableOnBackInvokedCallback`），但**验证机是 Android 11（vivo V2156A / SDK 30），
  该开关只对 Android 13+ 生效**，因此"左缘手势是否与背单词的右滑评价打架"这条风险**在本机不可观测**，
  未判定为通过；需要在 Android 13+ 设备上补测
- **baseline profile 的冷启动收益未实测**：本机 5 次 `am start -W -S` 为 1761 / 1764 / 1699 / 1685 / 618 ms
  （中位数 1699、最小 618，无 `ClassNotFound` / `VerifyError`，说明 profile 规则没写坏），
  但没有与「无 profile」构建做对照，因此**不声称任何启动收益**
- **词库商店的下载链路未在真机验通**：点「导入这本」后走到失败分支（商店顶部出现红色失败提示），
  原因文案已在这版补上（之前只写"可重试"，等于把 DNS / 明文策略 / 证书 / 404 压成同一个症状）。
  注意**商店能列出 81 本不能证明联网成功** —— 代码在目录拉取失败时会静默回落到打包进 APK 的
  `assets/dict/booklists.json` 快照，两者内容一模一样。所以"设备是否真的通网"本身也还是未知。
  因此整本词库下载**按未验证功能对待**（批量粘贴 / 文件 / 截图取词 / OCR 那几条已实测通过）
- 错题详情页「长按识别文字」未实现：详情页目前只能改学科与删除，没有编辑正文的入口

## [2.0.0] - 2026-09-19

### 新增
- **日/夜双主题**：`ui/theme` 重建为「暖纸感」双主题令牌层（`Palette` 原始色板 + `AppColors`/`AppTexts`
  经 `CompositionLocal` 下发），浅色暖纸 / 夜间暖黑各自达标，系统切换即时生效，状态栏跟着换
- **高帧率交互动效**：`MotionSpec` 统一 spring 规格（按压回弹 / 结算弹入 / 翻面 / 飞出），
  逐帧跟随 90/120Hz vsync；页面转场（淡入 + 短距位移）、底栏药丸指示、列表条目
  `animateItem()` 进出让位，全流程不再出现「整列瞬间跳一格」
- **滑动评价**：背单词卡片 3D 翻面 + 右滑「认识」/左滑「忘记」手势结算（阈值与甩速双判定，
  纯函数 `decideSwipe` 已补单测），答错选项抖动反馈
- **打卡热力图**：习惯页新增 GitHub 风格近 8 周热力图（`buildHeatmapCells` 跨月/跨年纯函数已补单测），
  打卡当日播彩带庆祝（`ConfettiBurst` 粒子合批），条目在待打卡/已打卡两区之间搬家带让位动画
- **达成与连续口径**：学习首页 hero 卡（今日待办进度环 + 连续学习火焰徽章）、正确率环形结算页、
  背单词小结卡数字滚动；连续天数由背单词与刷题共同驱动
- **墨水令牌**：`accentInk / successInk / goldInk / warningInk` 四个「文字与图标专用」变体
  （浅色压深后达 WCAG AA，夜间沿用提亮色），另有 `heatIdle`（热力图空格）与 `lightbox`（看图取景框）

### 变更
- **移除旧单主题令牌**：`DesignTokens` 整体下线，幸存的间距/圆角/阴影度量收进
  `AppTheme.space` / `AppTheme.radius` / `AppTheme.elevation`；颜色与文字样式一律取 `AppTheme`
- 错题本页新增「待复习 / 已掌握」两态筛选：标记掌握后条目从默认划线消失，已掌握清单仍可从上方 chip 回看
- 完成态、来源徽标等「色块 + 文字」处统一改为 `soft` 柔底 + `ink` 墨色（白字压实底只保留 accent 一处）
- 转屏不再重播已播放过的进场动画（`born` 等一次性标记改 `rememberSaveable`）

### 修复
- 刷题选项作答后仍可点击 / 触发 ripple 的锁定缺口；详情页返回出现「双弹」的转场竞态
- 品牌色作文字时不达 AA 的站点（判定文案、状态标签、百分比读数等）全部改用 ink 变体
- 夜间冷启动顶部「白条闪一下」：新增 `values-night/themes.xml`，窗口状态栏底色直接给夜间暖黑
  `#1C1B18`（浅色档同步从 v1 的 `#F8F8FA` 改为暖纸底 `#FAF8F2`；两档均仅 API < 35 生效，
  edge-to-edge 下由 Compose 承担）
- 启动图标残留的 iOS 蓝 `#007AFF` 改为品牌青绿 `#00A78E`
- 背单词结算与数据线程：拖动期整卡逐帧重组改为只在 `graphicsLayer` 内读动画值；错题/习惯/读书/
  日历四组的分组与解析型 `combine` 统一 `.flowOn(Dispatchers.Default)`，不再占主线程
- 打开错题详情不再可能把「标记掌握 / 删除 / 设复习时间」写到上一道题的行（渲染与动作都以路由
  `mistakeId` 为准，数据未就位时只出加载态）；读书详情、习惯日历的同类串台站点同步补挡
- 六个保存入口不再被双击重复写库、也不再多弹一层返回栈（ViewModel 侧一次性提交门）；
  错题录入页转屏不再丢失已输入内容；拍照路径改存字符串以便进程重建后回投
- 子页导航统一 `launchSingleTop`：连点「开始学习」「录入单词」不再多推一层（返回键不再「第一下没反应」）
- 读屏噪声：底栏图标不再与可见文字重复朗读、装饰性表情与「✓」退出朗读树、翻面卡任一时刻只读朝前的
  那一面、手写选择器补单选语义、热力图给出整块概览朗读

### 已知限制
- **未接系统预测式返回手势**（Android 13+ 的 `enableOnBackInvokedCallback` 未开启）：转场本身正确，
  但没有手势预览。需在真机验证系统手势后再开，列入 M2。（v2.1.0 已开启该开关；本项在 2.1.0 段仍标注为未实测）
- **120Hz 顺滑度为设计目标而非实测结论**：动效全部走 spring、重组热路径已收口，但本机无设备，
  帧率表现待真机走查。
- **边到边键盘补偿只是把 M3 的隐式默认写成显式约定**（`WindowInsets.safeDrawing` → `innerPadding`
  单一消费者），七张表单页在真机键盘下的表现待确认。
- baseline profile 只断言「产物内存在且非 0 字节」，无法逐条证明规则存活（AGP 仍可能静默丢弃）。
- 里程碑二（录入自动化五件套：批量粘贴 / 文件导入 / 在线词库 / 分享收件 / 截图 OCR）尚未开始。

### 构建
- CI 单元测试 101 项（热力图网格、月历补齐、连续天数、滑动判定、书脊色带、错题详情渲染、
  一次性提交门等纯函数）
- `versionCode = 2` / `versionName = "2.0.0"`
- 接入 `androidx.profileinstaller`，并把 `app/src/main/baseline-prof.txt` 重写为 **ART 文本 profile
  语法**（55 条规则，覆盖启动链、主题/动效令牌层、底栏导航、四 Tab 首帧与背单词热路径）；
  旧文件里 `Lcom/studykit/ui/theme/** { * }` 那类 R8 keep 写法会被 profile 解析器静默丢弃
- CI 新增 `release-build` job：`./gradlew --no-daemon assembleRelease` 校验 release（R8 混淆 +
  资源收缩）可编译，并用 `unzip -l` 断言产物内确有 `assets/dexopt/baseline.prof*`，缺失即 fail。
  仓库无 signingConfig，该 job 只产未签名包，**不签名、不上传、不发布**

## [1.0.0] - 2026-08-28

### 新增
- 四大功能模块：背单词 / 刷题、习惯打卡（含数量型目标与补卡）、读书笔记、错题整理
- 全局日历视图：聚合打卡记录与系统日历事件，按日查看明细
- 复习提醒：WorkManager 周期任务，到期错题与单词本地通知
- 学习数据导出：打卡记录 CSV 导出、成就文本分享
- Debug 构建首装自动播种演示数据（Release 构建不播种）

### 构建
- Kotlin 2.0.21 + Jetpack Compose（BOM 2024.12.01）+ Room 2.6.1（KSP）
- Gradle 依赖与插件版本统一收敛至 `gradle/libs.versions.toml` 版本目录
- CI：GitHub Actions（JDK 17 temurin），执行 lint / 单元测试 / Debug 构建
- Release 构建开启代码混淆与资源收缩（`isMinifyEnabled` / `isShrinkResources`）
  > 注意：混淆后的 release 包需在本地真机完整回归验证后方可发布。

### 修复
- 日历权限声明修正为官方 `READ_CALENDAR`（移除无效的 `READ_CALENDARS`）
- 删除错题时改为按 id 从数据库查询后再清理图片文件，不再依赖 UI 缓存
- 复习提醒的到期筛选下推至 SQL，避免全表载入内存过滤
