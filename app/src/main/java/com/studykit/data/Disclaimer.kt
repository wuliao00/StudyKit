package com.studykit.data

/**
 * 首次启动必读的免责声明。
 *
 * ## 为什么要带版本号
 *
 * [TEXT_VERSION] 与 `AppSettings.disclaimerVersion` 比对，不相等就再弹一次。
 * 条款改了而老用户看不到，等于没改 —— 一个只弹一次、且不认版本的"同意"，
 * 法律上和行为上都只是一次性的弹窗。
 *
 * ## 文案的取舍
 *
 * 只写**这个应用真实存在的**风险与边界，不抄通用模板：数据只在本机（没有服务器，
 * 所以"我们如何保护你的数据"这类句子在这里是假的）、换包与清库会丢数据（签名固定的 debug 包，
 * 所以卸载重装那条尤其要写）、在线词库来自第三方镜像、截图取词用系统识别不保证正确、
 * 提醒依赖系统通知可能被省电策略拦。
 */
object Disclaimer {

    /** 改文案就把它加一，老用户会再看到一次 */
    const val TEXT_VERSION = 1

    const val REPO_URL = "https://github.com/wuliao00/StudyKit"

    /** 一段一行，渲染时逐段排版（不写成一个大字符串：段落边界在代码里也要看得见） */
    val paragraphs: List<String> = listOf(
        "StudyKit 是个人开发的免费学习工具，按「现状」提供，不附带任何明示或默示担保。",
        "你的学习数据只存在这台设备上：单词、打卡、契约、错题全部写在本机数据库里，" +
            "不上传、不同步、没有服务器。卸载、清除数据、或换用签名不同的安装包都会丢失，" +
            "请定期到「设置 → 数据管理 → 导出备份」存一份。",
        "在线词库来自第三方公开词表与镜像，仅供个人学习使用，版权归原作者所有。" +
            "如果你是权利人且不希望被收录，可以在仓库提 issue 撤下。",
        "截图取词使用系统自带的文字识别，结果不保证正确，重要内容请自行核对。",
        "复习提醒依赖系统通知，可能因省电策略被延迟或拦截，不要把它当作唯一的提醒手段。",
        "使用本应用产生的任何后果由使用者自负。",
    )
}
